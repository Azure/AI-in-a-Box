# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: AIO-with-AI
metadata:
  template: AIO-with-AI@0.0.1
infra:
  provider: "bicep"
  path: "infra"
  module: "main"
hooks:
  preprovision:
    windows:
      shell: pwsh
      run: ./scripts/azd_preprovision.ps1
      continueOnError: false
      interactive: true
    posix:
      shell: sh
      run: ./scripts/azd_preprovision.sh
      continueOnError: false
      interactive: true