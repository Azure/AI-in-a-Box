{
    "name": "bot_show_image",
    "description": "Show an image from a URL. This is the only way you should ever show images. Do not attempt to embed it in a response in a different way.",
    "parameters": {
      "type": "object",
      "required": [
        "image_url"
      ],
      "properties": {
        "image_url": {
          "type": "string",
          "description": "The URL of the image to show."
        }
      }
    }
  }